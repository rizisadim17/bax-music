<nav class="bg-gradient-to-r from-blue-900 via-purple-900 to-green-900 shadow-lg border-b-2 border-green-400" data-controller="menu">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex items-center justify-between h-16">
            {# Logo/Brand #}
            <div class="flex items-center">
                <div class="text-green-400 text-xl font-bold">
                    🛸 Rick & Morty Portal
                </div>
            </div>

            {# Desktop Menu #}
            <div class="hidden md:block">
                <div class="ml-10 flex items-baseline space-x-4">
                    <a href="{{ path('search_characters_dimension') }}" 
                       class="px-3 py-2 rounded-md text-sm font-medium transition-all duration-200 
                              {% if app.request.get('_route') == 'search_characters_dimension' or app.request.get('_route') == 'characters_by_dimension' %}bg-green-600 text-white shadow-lg
                              {% else %}text-gray-300 hover:bg-gray-700 hover:text-white{% endif %}"
                       data-action="click->menu#setActive"
                       data-menu-section-param="dimensions">
                        🌌 Dimensions
                    </a>
                    <a href="{{ path('search_characters_location') }}" 
                       class="px-3 py-2 rounded-md text-sm font-medium transition-all duration-200 
                              {% if app.request.get('_route') == 'search_characters_location' or app.request.get('_route') == 'characters_by_location' %}bg-green-600 text-white shadow-lg
                              {% else %}text-gray-300 hover:bg-gray-700 hover:text-white{% endif %}"
                       data-menu-section-param="location">
                        📍 Locations
                    </a>
                    <a href="{{ path('characters') }}" 
                       class="px-3 py-2 rounded-md text-sm font-medium transition-all duration-200 
                              {% if app.request.get('_route') == 'characters' or app.request.get('_route') == 'character_show' %}bg-green-600 text-white shadow-lg
                              {% else %}text-gray-300 hover:bg-gray-700 hover:text-white{% endif %}"
                       data-action="click->menu#setActive"
                       data-menu-section-param="characters">
                        👥 Characters
                    </a>
                    <a href="{{ path('episodes') }}" 
                       class="px-3 py-2 rounded-md text-sm font-medium transition-all duration-200 
                              {% if app.request.get('_route') == 'episodes' or app.request.get('_route') == 'episode_show' %}bg-green-600 text-white shadow-lg
                              {% else %}text-gray-300 hover:bg-gray-700 hover:text-white{% endif %}"
                       data-action="click->menu#setActive"
                       data-menu-section-param="episodes">
                        📺 Episodes
                    </a>
                </div>
            </div>

            {# Mobile menu button #}
            <div class="md:hidden">
                <button 
                    data-action="click->menu#toggleMobile"
                    data-menu-target="mobileBtn"
                    class="inline-flex items-center justify-center p-2 rounded-md text-gray-400 hover:text-white hover:bg-gray-700 focus:outline-none focus:ring-2 focus:ring-inset focus:ring-white"
                    aria-label="Toggle menu">
                    <svg class="h-6 w-6" stroke="currentColor" fill="none" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
                    </svg>
                </button>
            </div>
        </div>
    </div>

    {# Mobile Menu #}
    <div data-menu-target="mobileMenu" class="md:hidden hidden">
        <div class="px-2 pt-2 pb-3 space-y-1 sm:px-3 bg-gray-800">
            <a href="{{ path('search_characters_dimension') }}" 
               class="block px-3 py-2 rounded-md text-base font-medium w-full text-left
                      {% if app.request.get('_route') == 'search_characters_dimension' or app.request.get('_route') == 'characters_by_dimension' %}text-white bg-green-600
                      {% else %}text-gray-300 hover:bg-gray-700 hover:text-white{% endif %}"
               data-action="click->menu#closeMobile">
                🌌 Dimensions
            </a>
            <a href="{{ path('search_characters_location') }}" 
               class="block px-3 py-2 rounded-md text-base font-medium w-full text-left
                      {% if app.request.get('_route') == 'search_characters_location' or app.request.get('_route') == 'characters_by_location' %}text-white bg-green-600
                      {% else %}text-gray-300 hover:bg-gray-700 hover:text-white{% endif %}"
               data-action="click->menu#closeMobile">
                📍 Locations
            </a>
            <a href="{{ path('characters') }}" 
               class="block px-3 py-2 rounded-md text-base font-medium w-full text-left
                      {% if app.request.get('_route') == 'characters' or app.request.get('_route') == 'character_show' %}text-white bg-green-600
                      {% else %}text-gray-300 hover:bg-gray-700 hover:text-white{% endif %}"
               data-action="click->menu#closeMobile">
                👥 Characters
            </a>
            <a href="{{ path('episodes') }}" 
               class="block px-3 py-2 rounded-md text-base font-medium w-full text-left
                      {% if app.request.get('_route') == 'episodes' or app.request.get('_route') == 'episode_show' %}text-white bg-green-600
                      {% else %}text-gray-300 hover:bg-gray-700 hover:text-white{% endif %}"
               data-action="click->menu#closeMobile">
                📺 Episodes
            </a>
        </div>
    </div>
</nav>